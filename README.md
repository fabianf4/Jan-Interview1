# For run this project:

You must be installed nodejs and npm.

Use `npm install` for download and install the dependencies.

Use `npm run dev` for run the project.

If you want the web to work correctly, you must run the API in the branch 3-api
